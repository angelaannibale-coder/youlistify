"use client";
import {useEffect,useState} from "react";
import {createClient} from "@supabase/supabase-js";
const supabase=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);
export default function WorkDetail({params}:{params:Promise<{id:string}>}){const [p,setP]=useState<any>(null),[loading,setLoading]=useState(true);useEffect(()=>{params.then(({id})=>supabase.from("work_posts").select("*").eq("id",id).single().then(({data})=>{setP(data);setLoading(false);}));},[params]);if(loading)return <main style={{padding:40}}>Loading...</main>;if(!p)return <main style={{padding:40}}><h1>Opportunity not found</h1><a href="/work">Back to Jobs, Gigs & Tasks</a></main>;const place=p.remote?"Remote":([p.city,p.state].filter(Boolean).join(", ")||"Location flexible");const amount=p.pay_amount?Number(p.pay_amount).toLocaleString("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0}):"";const pay=p.pay_type==="contact"?"Pay: discuss details":p.pay_type==="hourly"?`${amount}/hr`:p.pay_type==="salary"?`${amount} salary`:amount;return <main style={{minHeight:"100vh",background:"#f8f8fb",padding:"40px 18px",fontFamily:"Arial,sans-serif"}}><article style={{maxWidth:800,margin:"0 auto",background:"white",padding:"clamp(26px,5vw,46px)",borderRadius:24,boxShadow:"0 10px 35px rgba(0,0,0,.07)"}}><a href="/work" style={{color:"#5b4df5",fontWeight:800,textDecoration:"none"}}>← Jobs, Gigs & Tasks</a><div style={{marginTop:24,color:"#5b4df5",fontWeight:900,textTransform:"uppercase",letterSpacing:1}}>{p.post_type}</div><h1 style={{fontSize:"clamp(34px,6vw,50px)",lineHeight:1.08,color:"#171b36",margin:"10px 0"}}>{p.title}</h1><div style={{display:"flex",gap:14,flexWrap:"wrap",color:"#667085",fontWeight:700}}><span>📍 {place}</span>{p.category&&<span>✦ {p.category}</span>}<span>💵 {pay}</span></div><div style={{height:1,background:"#ececf2",margin:"28px 0"}}/><h2>What they need</h2><p style={{whiteSpace:"pre-wrap",fontSize:17,lineHeight:1.7,color:"#454b60"}}>{p.description}</p><div style={{height:1,background:"#ececf2",margin:"28px 0"}}/><h2>Respond</h2><div style={{display:"flex",gap:10,flexWrap:"wrap",marginTop:16}}>{p.contact_call&&p.contact_phone&&<a href={`tel:${p.contact_phone}`} style={btn}>Call</a>}{p.contact_text&&p.contact_phone&&<a href={`sms:${p.contact_phone}`} style={btn}>Text</a>}{p.contact_email&&p.contact_email_address&&<a href={`mailto:${p.contact_email_address}?subject=${encodeURIComponent("YouListify: "+p.title)}`} style={btn}>Email</a>}{p.application_url&&<a href={p.application_url.startsWith("http")?p.application_url:`https://${p.application_url}`} target="_blank" rel="noreferrer" style={btn}>Apply / Website</a>}{p.contact_youlistify&&<a href={`mailto:${p.contact_email_address||""}?subject=${encodeURIComponent("YouListify response: "+p.title)}`} style={{...btn,background:"#5b4df5",color:"white"}}>Contact through YouListify</a>}</div></article></main>}
const btn={padding:"13px 18px",borderRadius:11,border:"1px solid #d9dce7",textDecoration:"none",fontWeight:800,color:"#4f46e5",background:"white"};
